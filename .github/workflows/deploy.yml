name: 🚀 Deploy & Quality Assurance


on:
  push:
    branches:
     - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write 


concurrency:
  group: ${{github.workflow}}-${{github.ref}}
  cancel-in-progress: true 


jobs:
  deploy:  
    name: 🚀 Deploy to Ghithub pages
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    outputs:
      page_url: ${{steps.deployment.outputs.page}}


    enviroment:
      name: github-pages
      url: ${{steps.deployment.outputs.page_url}}

    steps:
      - name: 📥 checkout code
        uses: actions/checkout@v4

      - name: 📄 Setup pages
        uses: actions/configure-pages@v4
     
      - 
  